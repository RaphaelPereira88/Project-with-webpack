import { postData } from "../src/client/js/postData"

describe("Testing the update function", () => {
    test("if returned data is correct  ", ()=> {
            const input ={sentence:"https://www.bbc.co.uk/news/science-environment-56799755"}
            const output = ? a promise ? 
            expect(postData('http://localhost:8081/clientdata',input)).toBe(output)
    });
});

? a promise ?  : i don't know how to write this down 